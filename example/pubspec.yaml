name: text_to_pixel_art_example
description: Example app demonstrating text_to_pixel_art package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  text_to_pixel_art:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # 멀티플랫폼 지원을 위한 설정
  # 이 앱은 Android, iOS, Web, macOS, Windows, Linux에서 실행 가능합니다 